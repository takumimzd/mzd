import { MARGIN } from "@/constants/style";
import { style } from "@vanilla-extract/css";

export const homeStyle = {
  wrapper: style({
    margin: MARGIN.L,
  }),
  tabWrapper: style({
    marginTop: MARGIN.L,
  }),
};
